
spring.application.name: formato
spring:
  application:
    name: formato-service
  datasource:
    url: jdbc:mysql://localhost:3306/tallerweb
    username: root
    password: secret
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  #server:
  #  port: 8081
